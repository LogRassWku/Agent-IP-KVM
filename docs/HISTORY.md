# Agent IP KVM 项目历史

本文档记录项目的重要决策、验证结果和方向变化。功能完成情况以对应需求的验收结果为准。

## 记录规则

- 只记录已经发生的事件，不把计划写成完成状态。
- 每条记录包含日期、事件、依据和影响。
- 硬件实验应记录设备类型、测试条件、结果和未解决问题。
- 涉及方向或范围变化时，说明保留了什么、调整了什么。
- 不记录密码、密钥、私有网络地址、队友屏幕内容等敏感信息。

## 2026-09-05：项目构想形成

### IP KVM 方向

- 确定以开发板、视频采集和 USB HID 组成软硬一体 IP KVM。
- 明确开发板自带 HDMI 接口是输出口，采集被控电脑画面需要独立的视频输入设备。
- 项目目标包含操作系统运行前的画面和控制能力，因此未来需要支持 BIOS／UEFI 和系统安装环境。

### 双 Agent 构想

- 提出板端 KVM Agent 与可选 PC Agent 协作的方案。
- KVM Agent 独立于被控电脑的操作系统，负责画面观察、权限检查、有限执行和结果验证。
- PC Agent 仅在用户选择安装后运行，负责读取经过授权的系统内部信息并提供建议。
- BIOS 修改、磁盘操作、系统安装和删除数据等高风险动作必须展示具体计划，并由用户明确批准。
- PC Agent 的断连停用和自清理必须基于安装清单，只能处理能够证明属于本项目的文件。

## 2026-09-05：首个参考开发板建立基线

- 确认首个参考平台为 D-Robotics RDK X5 V1.0。
- 确认系统为 Ubuntu 22.04.5 LTS、ARM64、Linux 6.1.83。
- 配置独立远程管理凭据，并完成标准系统更新和软件包完整性检查。
- 具体网络地址和认证配置仅保存在本地环境，不进入公开仓库。

## 2026-09-05：首款 USB 采集卡验证

### 已确认

- RDK X5 能够识别采集卡的 USB 视频和音频接口。
- Linux `uvcvideo` 驱动能够自动加载，并创建设备节点。
- 设备声明支持 MJPEG、多种分辨率和最高 60 fps。

### 未通过

- 720p/30、1080p/30 和 1080p/60 均未取得可解码的有效帧。
- V4L2、FFmpeg 和 OpenCV 三条读取路径均收到空数据。
- 内核记录 UVC USB 协议错误 `-71`。
- 更换 RDK X5 USB Type-A 接口和 HDMI 线后现象不变。
- 被控笔记本没有检测到采集卡对应的外接显示器，HDMI 热插拔／EDID 握手可能没有建立。

### 结论

- 设备枚举成功不能作为视频链路成功的依据。
- 当前采集卡需要继续交叉测试或更换，不能作为第一版稳定硬件基线。
- 该问题不阻止平台无关的软件接口设计，但真实画面闭环仍是首版验收的必要条件。

## 2026-09-05：调整为多开发板项目

- 项目从 RDK X5 单板实现调整为可部署到多种 Linux 开发板的软件与硬件方案。
- RDK X5 保留为首个参考平台。
- 核心逻辑不得依赖板载 HDMI 输入，也不得硬编码 RDK X5 的设备路径。
- 视频输入、USB HID 和硬件加速通过独立适配层接入。
- CPU 基础路径必须可用，BPU、GPU 和其他 NPU 作为可选加速后端。

## 2026-09-05：开源仓库建立

- 建立公开 GitHub 仓库 `LogRassWku/Agent-IP-KVM`。
- 加入 README、Apache License 2.0、忽略规则和项目计划。
- 仓库名称由误写的 `Agenet-IP-KVM` 更正为 `Agent-IP-KVM`。
- 本地 `main` 与 GitHub `main` 已同步。

## 2026-09-05：视频源软件基线建立

- 使用 Python 3.10 标准库建立平台无关的视频源接口，定义能力查询、打开、启动、取帧、停止、关闭和健康状态。
- 加入 1280×720、30 fps、RGB24 模拟彩条视频源，使无采集硬件环境也能验证核心取帧流程。
- 最小验证命令连续取得 30 个完整帧，帧序号为 0 至 29，总数据量 82,944,000 字节，实测约 30.46 fps，结束状态为 `closed`。
- 生命周期和错误状态测试通过。测试结果只证明软件接口闭环，不代表真实 HDMI/UVC 链路已经恢复。
- 下一项软件工作是实现 Linux V4L2 视频源适配器；该适配器可以先完成设备发现和能力查询，真实取帧仍受当前采集硬件问题影响。

## 2026-09-05：V4L2 设备探测实现

- 加入 Linux V4L2 探测模块，通过 `v4l2-ctl` 查询 `/dev/video*` 节点，不启动视频流。
- 输出设备路径、名称、驱动、总线、视频采集支持状态，以及离散的像素格式、分辨率和帧率组合。
- 对非 Linux 平台、无设备、缺少 `v4l2-ctl` 和单节点查询失败提供结构化状态；单个坏节点不会隐藏其他节点。
- 七项自动测试全部通过，其中五项覆盖 V4L2 信息解析、格式解析、无设备状态、平台不支持状态和模拟设备发现。
- Windows 实测正确返回 `unsupported_platform`。
- RDK X5 实机确认 Linux 环境与 `/usr/bin/v4l2-ctl` 可用；采集设备未连接时，统一命令正确返回 `no_devices`，没有把无设备误报为程序故障。
- 接入已确认故障的采集卡后，统一命令发现两个节点：`/dev/video0` 正确归类为视频采集节点并列出 30 个 MJPEG 模式，`/dev/video1` 正确归类为元数据节点且不声明视频格式。
- 首次实机探测暴露了总体能力与单节点能力混淆的问题；解析逻辑改为优先使用 `Device Caps`，新增元数据节点测试后共八项测试通过。

## 2026-09-05：确定故障采集卡的替代测试路线

- 当前采集卡只用于设备枚举、驱动识别、节点分类和格式查询，不再用于证明真实画面链路。
- 软件取帧、状态管理和后续浏览器传输先使用模拟源，并计划加入视频文件源。
- V4L2 真实取帧验收将使用普通 USB 摄像头或其他已知正常的 UVC 设备；这一测试只要求产生真实非空帧，不要求输入来自 HDMI。
- RDK X5 已安装 FFmpeg，但没有 `v4l2loopback` 模块和对应内核构建目录。当前不安装 DKMS 模块，避免在定制内核上引入额外变量。
- NanoKVM PCIe 保留为 IP KVM 功能参考和未来网络视频源测试设备，不能直接替代 RDK X5 的 UVC 采集输入。

## 2026-09-05：确定首版 Web 界面顺序

- 视频文件源完成后先实现最小 Web 视频界面，再开发 USB HID。
- 页面主体显示视频并保持宽高比；顶部工具栏固定显示，不采用 NanoKVM 的鼠标悬停后出现方式。
- 工具栏第一版只放已经可用的视频状态、播放控制、重新连接、全屏和设置入口，不加入尚未实现的功能按钮。
- 设置面板第一版只读展示视频后端、设备、驱动、节点类型、格式、分辨率、帧率、健康状态和最近错误。
- 首次验收使用模拟源和视频文件源，使界面开发不受故障 HDMI 采集卡阻碍。

## 2026-09-05：视频文件源完成

- 加入基于 FFmpeg 的本地视频文件源，通过 `ffprobe` 读取第一条视频轨，并统一输出 RGB24 帧。
- 文件源支持能力查询、打开、启动、连续取帧、停止、关闭和自然结束状态，与模拟源使用同一接口。
- 本地自动测试增加到十一项，覆盖分数帧率解析、完整帧读取、连续帧序号、文件结束和文件不存在错误。
- 在 RDK X5 临时生成 320×180、10 fps、两秒的无隐私测试视频；成功读取前 10 帧，序号为 0 至 9，总数据量 1,728,000 字节。
- 请求读取 25 帧时，在第 20 帧后正确返回 `video file reached end of stream`，不会伪造后续画面。
- 视频文件源达到当前验收标准，下一阶段进入最小 Web 视频界面。

## 2026-09-05：最小 Web 页面骨架完成

- 使用 Python 标准库建立只读 Web 服务，不增加第三方 Web 框架依赖。
- 页面包含固定顶部工具栏、视频显示区域、来源状态、刷新、全屏和设置按钮；设置面板从状态接口读取当前视频源和 V4L2 设备信息。
- 页面在窄屏环境缩减按钮文字并保留图标；设置面板支持按钮、遮罩和 Escape 键关闭。
- 服务默认监听 `127.0.0.1`。RDK X5 临时预览仅监听 USB QuickLink 管理地址和 8765 端口。
- RDK X5 状态接口确认文件源为 320×180、10 fps、RGB24，并正确列出故障采集卡的视频节点和元数据节点。
- 自动测试增加到十二项，覆盖首页和结构化状态接口。
- 页面当前显示明确的无画面状态；下一小步接入连续视频画面。

## 2026-09-05：简化无信号界面

- 根据浏览器实机截图，删除无画面区域中的状态胶囊、摄像机图标、中文标题、说明文字和底部视频源路径。
- 无画面时只显示“小圆点 + No Signal”；视频源可用时圆点为绿色，不可用或服务断开时为红色。
- 刷新设备状态期间不再临时变成黄色，保留上一次已知颜色直到查询完成。
- 删除顶部工具栏左侧的绿色 `A` 品牌图标；宽屏保留纯文字名称，窄屏保留简洁工具栏。
- 修改已部署到 RDK X5 临时预览，HTTP 页面确认包含 `No Signal` 且旧元素已移除。

## 2026-09-05：确定双模式交互与板端模型基线

### 双模式交互

- 产品界面划分为 KVM 模式和 Agent 模式。
- KVM 模式向用户显示实时画面，并允许经过授权的浏览器会话使用键盘和鼠标。
- Agent 模式隐藏实时画面并停用用户的浏览器键鼠输入，用户只通过自然语言提交任务；板端 Agent 仍可在权限策略约束下观察画面和执行动作。
- Agent 模式采用按需截图、画面变化检测和动作后复核，不要求持续高帧率。等待稳定界面时可降到每秒 0.2 至 2 次观察，快速界面变化期间再临时提高采样频率。

### 板端模型实测

- RDK X5 实测为 8 核 Cortex-A55，系统可用内存约 6.9 GiB，安装前可用内存约 6.6 GiB，根文件系统剩余约 20 GiB。
- 确定首个板端模型为官方 `Qwen2.5-1.5B-Instruct-GGUF` 的 `Q4_K_M` 量化版，运行参数为 2048 tokens 上下文和 4 个 CPU 线程。
- 模型文件大小为 1,117,320,736 字节，SHA-256 为 `6a1a2eb6d15622bf3c96857206351ba97e1af16c30d7a74ee38970e434e9407e`。
- 在 Ubuntu 22.04 ARM64 上从 `llama.cpp v0.4.0` 源码构建 CPU 运行时；官方同期 ARM64 预编译包要求 `GLIBC_2.38` 和 `GLIBCXX_3.4.32`，与当前系统不兼容，因此没有采用。
- 首次只读测试能够按要求输出 `action`、`risk` 和 `requires_confirmation` JSON。实测提示处理约 9.7 tokens/s，生成约 3.9 tokens/s，包含模型冷启动的一次回答约 29.3 秒。
- 模型和运行时安装在普通用户目录，没有写入系统目录；当前只验证命令行推理，常驻本地 API 和项目内模型适配器尚未接入。

## 2026-09-05：浏览器连续视频闭环完成

- Web 服务加入共享 MJPEG 视频流。服务只读取和编码一份视频，各浏览器接收同一组最新帧，避免每个观看者分别打开采集设备。
- 页面加入保持原始宽高比的视频元素；收到首帧后隐藏 `No Signal`，视频结束、读取失败或编码失败时恢复无信号状态。
- 视频流延迟到首个浏览器请求时启动，状态接口报告 `idle`、`starting`、`streaming`、`ended`、`error` 或 `stopped`，同时给出最近帧序号和错误。
- 使用持久 FFmpeg 进程把统一 RGB24 帧编码为 JPEG，避免每帧创建一个新进程；编码限定为 4 线程帧内分片，防止多帧并行造成首帧等待。
- RDK X5 使用 1280×720、30 fps 模拟源实测，浏览器成功显示完整彩条画面。5 秒网络采样取得 23 个完整 JPEG 帧，CPU 通用编码路径约 4.6 fps，FFmpeg 约使用 1.26 个 CPU 核。
- 当前性能只作为 RGB24 软件转码基线。后续正常 UVC 设备若输出 MJPEG，应增加压缩帧直通路径，避免无意义的解码和重新编码。
- 自动测试增加到十四项；RDK X5 部署前的十三项测试和电脑端最终十四项测试全部通过。新增测试验证 MJPEG 响应边界、JPEG 内容、帧序号和视频结束状态。

## 2026-09-05：RDK X5 USB HID 能力只读探测完成

- 加入平台无关的只读 USB Gadget HID 探测工具，报告 UDC、ConfigFS、内核 HID 支持、现有 Gadget 功能和管理链路风险；工具不会修改 ConfigFS 或发送输入事件。
- RDK X5 实测暴露 `35300000.usb`，状态为 `configured`，当前和最高速度均为 USB High-Speed；ConfigFS 已挂载，内核启用 `CONFIG_USB_GADGET=y`、`CONFIG_USB_CONFIGFS=m` 和 `CONFIG_USB_CONFIGFS_F_HID=y`。
- 当前系统 Gadget `g_comp` 已绑定 `RNDIS`、`ECM` 和只读 Mass Storage，USB QuickLink 管理地址位于 `usb0`。探测工具因此返回 `in_use` 和 `safe_to_modify_now: false`。
- 厂商脚本已经包含 `rndis-hid` 配置和 HID FunctionFS 创建逻辑，说明平台能够建立 HID Gadget；但随系统提供的 34 字节报告描述符定义的是厂商私有用途页面，报告长度为 1024 字节，不是标准键盘或鼠标。
- 第一版真实 HID 需要使用项目自有的标准键盘和鼠标描述符，并将其加入保留管理网络的复合 Gadget。任何重绑都会导致 USB 重新枚举，实施前必须准备本地备用访问和恢复脚本。
- 三项 HID 探测测试加入后，项目自动测试总数为十七项；RDK X5 实机探测与电脑端完整测试均通过。

## 2026-09-05：HID 模拟适配层完成

- 建立平台无关的 HID 输出接口，统一键盘按下／释放、相对鼠标移动、鼠标按钮、全部释放、紧急停止、重新启用和关闭语义。
- 加入纯内存模拟后端，只记录动作和当前按下状态，不访问 ConfigFS、`/dev/hidg*` 或任何连接的电脑。
- 紧急停止会先清空全部按键和鼠标按钮，再进入拒绝输入状态；必须显式重新启用才能继续。正常关闭和异常退出同样释放全部输入。
- 相对鼠标单次位移限制为标准 8 位报告可表示的 `-127` 至 `127`，后续大距离移动由上层拆分，避免不同硬件后端产生溢出差异。
- 根据实机页面反馈，顶部栏删除可见的 `Agent IP KVM` 标题，只保留右侧视频工具按钮；修改已部署至 RDK X5 预览。
- 新增四项 HID 生命周期测试后，项目自动测试总数为二十一项，全部通过。

## 2026-09-05：标准 HID 描述符和离线复合配置完成

- 加入标准 8 字节 USB Boot Keyboard 报告描述符，以及支持五个按钮、X／Y 相对移动和垂直滚轮的 4 字节鼠标报告描述符。
- 加入只输出 JSON 的复合 Gadget 配置生成器。输出包含描述符长度、SHA-256、ConfigFS 参数、现有功能、计划功能和重绑风险，不包含执行或写入逻辑。
- RDK X5 实机生成计划保留 `ecm.0`、`mass_storage.0`、`rndis.0`，并增加 `hid.keyboard`、`hid.mouse`；计划正确标记 `requires_rebind: true` 和 `requires_local_recovery: true`。
- 实机生成期间未写入 ConfigFS，UDC 和 USB QuickLink 管理连接保持原状。
- 新增两项描述符与配置计划测试后，项目自动测试总数为二十三项，全部通过。

## 2026-09-05：HID 本地恢复文件包完成

- 加入恢复文件包生成器，输出 `manifest.json`、`preflight.sh`、`rollback.sh` 和 `LOCAL_RECOVERY.md`，不生成或执行 HID 应用脚本。
- 预检查脚本只读取 Gadget、UDC、配置和现有功能，发现状态与清单不一致时拒绝继续。
- 回滚脚本默认进入 dry-run，只展示将要移除的两个项目 HID 功能；必须显式传入 `--apply` 且使用管理员权限才会修改 ConfigFS。
- 实际回滚只处理 `hid.keyboard` 和 `hid.mouse` 的精确链接与功能目录，保留生成时记录的网络和只读存储功能；退出陷阱会尝试重新绑定原 UDC。
- 本地恢复说明要求提前建立不依赖 USB QuickLink 的串口或本地终端会话。文件包拒绝覆盖非空目录，避免破坏已有恢复资料。
- 新增两项恢复包测试后，项目自动测试总数为二十五项，全部通过。RDK X5 在 `/tmp` 成功生成临时恢复包，两个脚本通过 shell 语法检查，只读预检查返回 `PASS`，回滚默认模式返回 dry-run；验证后 UDC 仍绑定 `35300000.usb`，Web 状态仍为 `streaming / available`，没有实际解绑或重绑 USB。

## 2026-09-05：标准键盘与鼠标短时枚举通过

- 在当前 Windows 开发电脑上记录枚举前基线：RDK X5 提供复合设备、RNDIS 网卡、只读存储和 Windows 不支持的 ECM 接口，没有键盘或鼠标接口。
- 恢复包加入 `temporary-apply.sh` 和两份二进制报告描述符。脚本默认 dry-run；显式应用时先启动独立回滚看门狗，再修改 ConfigFS，超时时间限制为 20 至 300 秒。
- 前三次受保护尝试因 ConfigFS 链接方式和 Windows OS Descriptor 配置顺序被拒绝；每次均立即恢复原 UDC、网络和存储功能，没有生成 HID 设备，也没有向电脑发送输入。
- 通过未绑定的临时 Gadget 确认 ConfigFS 需要以配置目录作为 `ln` 目标；进一步确认修改功能链接前需要暂时移除 `os_desc/c.1`，完成后再恢复该链接。
- 第四次 45 秒测试成功。Windows 识别 `HID Keyboard Device` 和 `HID-compliant mouse`，两个接口状态均为正常；RNDIS、复合设备和只读存储同时保持正常。
- 测试期间没有打开 `/dev/hidg*`，因此没有发送键盘或鼠标报告。USB 重新枚举导致原 SSH 会话重置，符合预期。
- 45 秒后看门狗自动执行精确回滚。Windows 的 HID 接口消失；开发板只剩 `ecm.0`、`mass_storage.0`、`rndis.0`，`os_desc/c.1` 和 `35300000.usb` 绑定均恢复，Web 状态为 `streaming / available`。

## 2026-09-05：Linux USB Gadget 全零释放报告通过

- 实现 Linux USB Gadget HID 输出后端，支持标准键盘、五键相对鼠标、全部释放、紧急停止和重新启用生命周期。
- 后端读取 ConfigFS HID 功能的主／次设备号，并据此匹配 `/dev/hidg*`，不硬编码键盘和鼠标的节点序号。
- 独立验证命令必须显式传入 `--release-only`，当前没有暴露发送有效按键、点击或移动的命令行入口。
- 第一次写入在 USB 重新绑定后立即执行，主机尚未完成接口配置，内核对两个端点均返回 `Errno 108`；程序没有发出有效动作，自动回滚正常完成。
- 加入 2.5 秒主机配置等待后再次实测成功：键盘 `/dev/hidg0` 写入 `0000000000000000`，鼠标 `/dev/hidg1` 写入 `00000000`，结果明确记录 `active_input_sent: false`。
- Windows 同时识别并启动项目 HID 键盘与鼠标。45 秒后看门狗移除两个接口，开发板恢复 `ecm.0`、`mass_storage.0`、`rndis.0`、原 OS Descriptor 链接和 `35300000.usb` 绑定；Web 继续为 `streaming / available`。
- 新增五项 Linux HID 输出测试后，项目自动测试总数为三十项，全部通过。

## 2026-09-05：Web 工具栏预留 HID 入口

- 顶部工具栏增加鼠标和键盘入口，为后续 KVM 控制功能保留固定位置。
- 两个入口在认证、单一控制者和紧急停止流程完成前保持禁用，避免误发键鼠输入。
- 原全屏入口改为屏幕按钮和显示器图标，现阶段继续切换视频区域的全屏显示。

## 2026-09-05：真实 HDMI 画面进入 Web 界面

- 新 UGREEN 25854 在 RDK X5 上识别为 UVC 设备：`/dev/video0` 为视频采集节点，`/dev/video1` 为元数据节点。
- 首次取帧显示采集卡无信号页；笔记本反复出现显示器接入动画。开发板侧确认采集卡处于 USB 自动休眠状态，临时将 `power/control` 设为 `on` 后 HDMI 握手稳定。
- 成功取得并本地检查 1920×1080 的真实 Windows 桌面帧。截图保存在 Git 忽略的 `work/` 目录，不提交画面内容。
- 实现 FFmpeg V4L2 MJPEG 视频源和浏览器直通路径，不在 RDK X5 上重复解码、编码采集卡已经生成的 JPEG。
- Web 服务切换为 `/dev/video0`、1920×1080、30 fps；状态连续增长，2 秒记录 62 帧，后端报告 `streaming / available`。
- 自动测试由三十项增加到三十二项，全部通过。

## 2026-09-05：屏幕模式菜单与采集卡保持唤醒

- 屏幕按钮改为锚定在按钮下方的小窗口，提供分辨率和刷新率两个选择框。
- 选项来自当前 V4L2 视频节点声明的 MJPEG 模式；后端在应用前重新查询并验证该组合，拒绝设备不支持的参数。
- 切换模式时停止旧视频源、断开旧浏览器流并等待页面重新连接，避免两个 FFmpeg 进程同时占用 `/dev/video0`。
- RDK X5 实测从 1920×1080、30 Hz 切换到 1280×720、60 Hz，取得有效 1280×720 JPEG；随后恢复 1920×1080、30 Hz 并取得有效 JPEG。
- 新增 `install-uvc-power-rule.sh`，安装只匹配 `2b89:5854` 的 udev 规则，并支持 `--remove` 撤销。RDK X5 当前状态为 `active / on`。
- 自动测试由三十二项增加到三十三项，全部通过。

## 2026-09-05：画面缩放与 Web 屏幕键盘完成

- 屏幕菜单增加 50% 至 200% 的本地画面缩放；缩放只改变视频画面，固定顶部工具栏的位置和大小不变。
- 鼠标按钮增加三档浏览器光标大小设置。当前没有接入鼠标移动、点击或滚轮发送。
- 键盘按钮增加适合触屏使用的屏幕键盘，包含字母、数字、常用符号、功能键、方向键及一次性 Ctrl／Shift／Alt／Win 组合键。
- Web 服务增加受限的单次按键接口。每次请求均按“组合键按下、主键按下、主键释放、组合键释放、全部释放”的顺序执行，并拒绝未列入标准键盘映射的键值。
- HID 后端必须通过 `--enable-hid` 显式启用；默认部署仍不发送输入。页面在后端未启用时允许打开屏幕键盘查看布局，但按键保持不可用。
- 新增三项 Web HID 测试后，项目自动测试总数为三十六项，全部通过。当前只完成模拟后端闭环，真实有效按键仍需在受保护的 USB Gadget 测试窗口中验收。

## 下一条记录

发生以下任一事件时追加记录：

- 新采集卡或当前采集卡首次取得有效画面。
- 确定第一版技术栈与目录结构。
- 完成第一个视频采集适配器。
- 完成第一个 USB HID 无破坏性按键测试。
- 完成 USB HID 适配层和无破坏性键鼠测试。
- 项目范围、安全规则或目标平台发生变化。
